import { useTheme } from "@/hooks/useTheme";
import { Pressable, StyleSheet, View } from "react-native";

export function ParameterButton(){
    const theme = useTheme()
    return (
        <Pressable>
            
        </Pressable>
    )
}

const style = StyleSheet.create({
    contenair: {

    }
})